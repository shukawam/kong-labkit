# yaml-language-server: $schema=../schemas/env.schema.json
customer: acme
gateway: ai-gateway-v2
control_plane: konnect
region: us

ai:
  providers:
    - type: azure
      instance: acme-foundry
      auth: api-key
      models:
        - name: gpt-5-6
          deployment_id: gpt-5.6
          api_version: 2024-12-01-preview
  semantic_cache: true

cache:
  type: redis-stack

vectordb:
  type: redis-stack
